<div class="container">
	<ul class="nav nav-tabs nav-justified">
		<li role="presentation"><%= link_to "#{@board.name} Job Board", root_path %></li>
	  <li role="presentation"><%= link_to 'Post Jobs', new_listing_path %></li>
	  <% unless employer_signed_in? %>
		  <li role="presentation" class="dropdown">
		    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
		      <%= current_user ? "Logged in as #{current_user.email}" : 'User' %><span class="caret"></span>
		    </a>
		    <ul class="dropdown-menu">
		    <% if user_signed_in? %>
					<li role="presentation"><%= link_to 'Edit Profile', edit_user_registration_path, role: 'menuitem', tabindex: '-1'%></li>
					<li role="presentation"><%= link_to 'Log Out', destroy_user_session_path, method: :delete, role: 'menuitem', tabindex: '-1'%></li>
				<% else %>
			    	<li role="presentation"><%= link_to 'Sign Up', new_user_registration_path, role: 'menuitem', tabindex: '-1'%></li>
					<li role="presentation"><%= link_to 'Login', new_user_session_path, role: 'menuitem', tabindex: '-1'%></li>
			    <% end %>
		    </ul>
		  </li>
	  <% end %>
	  <% unless user_signed_in? %>
		  <li role="presentation" class="dropdown">
		    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
		      Employers <span class="caret"></span>
		    </a>
			<ul class="dropdown-menu">
		    	<% if employer_signed_in? %>
					<li role="presentation"><%= link_to 'Edit Profile', employer_profile_path, role: 'menuitem', tabindex: '-1'%></li>
					<li role="presentation"><%= link_to 'Log Out', destroy_employer_session_path, method: :delete, role: 'menuitem', tabindex: '-1'%></li>
				<% else %>
			      	<li role="presentation"><%= link_to 'Sign Up', new_employer_registration_path, role: 'menuitem', tabindex: '-1'%></li>
					<li role="presentation"><%= link_to 'Login', new_employer_session_path, role: 'menuitem', tabindex: '-1'%></li>
			    <% end %>
		    </ul>
		  </li>
	  <% end %>
	</ul>
</div>