<%= render partial: 'employer_dashboard' %>

<% @posts.each do |post| %>
	<%= link_to post_path(post) do %>
		<div class="container">
			<div class="row">
				<h1><%= post.title %></h1>
			</div>
		</div>
	<% end %>
<% end %>